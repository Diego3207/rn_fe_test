
module.exports = {
    
  friendlyName: 'Disable',
	description: 'Disable Purchase_order',
    
    
    
  inputs: 
	{   
    id: {  // purchase_order_id varchar(250)  NOT  NULL
      type: 'number',
      required: true,
    },

  },
	exits: 
	{
        error: 
        {
          statusCode: 500,
          description: 'Error general',
        },
        
    },
    fn: async function (inputs, exits) 
	{
    
      try {
        
    
        var disable =  await PurchaseOrder.update({
            id: inputs.id
        }).set({
          purchaseOrderActive: false
        });
        return exits.success
          ({           
            message: `Se mandó a papelera`,
          }); 
       
      }
      catch(error){
            return exits.error
            ({
                //message: `Error logging in user ${inputs.email}`,
                error: error.message,
            });
        }
    }
}